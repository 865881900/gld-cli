import NewProject from './NewProject';

export default class NodeNewProject extends NewProject {
  constructor(projectName: string, newProjectPath: string) {
    super(projectName, newProjectPath);
  }
}
